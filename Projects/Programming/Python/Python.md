Пока смотрю https://www.youtube.com/watch?v=wDmPgXhlDIg
- Код исполняется сверху вниз
- Переменная создается просто. Нужно взять именованный идентификатор и присвоить ему значаение  `name = 'Имя'` еще можно так `x, y = y, x` теперь `x` имеет значение `y`, а `y` наоборот 
`print()` – это как console.log() в js
`input()` - Ожидает ввода в терминал
`type()` – Помогает узнать тип переменной

Существует некоторые договоренности для нейминга файлов
- `snake_case` – для функций, переменных или методов
- `CamelCase` – для классов
- `kebab-case` – для пакетов (типа npm я думаю)
- `UPPERCASE` - для констант

## Типы данных
### Числа
`int` – целочисленное значение
`float` – значение с плавающей точкой

### Строки



## Условные операторы
```python
x = 1
y = 1

if x > 0 and y > 0:
    print('Positive')
elif x < 0 and y < 0:
	print('Negative')
else:
    print('Equal')
```

---
**Источник:** 
**Теги:**
**Связи:** 
**Дата:** 20.01.2024